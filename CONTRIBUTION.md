# 贡献要求

感谢你参与改进本项目。以下要求用于减少回归问题，同时尽量保持贡献流程简单。

## 1. 本地检查

请先安装 [uv](https://docs.astral.sh/uv/) 和 Node.js，然后在项目根目录运行：

```bash
uv sync
uv run python scripts/check.py
```

该命令会进行：

- Python 单元测试。
- JavaScript 语法检查。
- 存在 `extension/core.test.js` 时运行 JavaScript 单元测试。
- Python 编译检查。
- Git 差异格式检查。

提交前应确保检查通过。新增功能或修复缺陷时，**请尽量添加对应测试**；缺陷测试应能覆盖原来的失败场景，防止以后再次出现。

纯文档修改通常不需要新增测试，但仍建议运行统一检查。

## 2. 实机验证

登录、课程接口和浏览器下载依赖真实环境，无法完全由自动测试验证。改动涉及 CLI 抓取、校内站点、WebVPN 或浏览器扩展时，请提供**截图或简短录屏**，至少展示：

- 修改后的功能可以正常完成。
- 原有相关功能没有被破坏。

实机验证只需覆盖本次改动直接影响的主要流程，不要求完整测试项目的所有功能。纯文档、测试代码或不改变运行行为的整理不需要提供截图或录屏。

请隐藏账号、Cookie、认证信息、课程隐私信息和实际字幕内容。如果暂时无法访问某个环境，请在 PR 中说明未验证的场景。

## 3. Pull Request 说明

PR 描述请简要写明：

- 修改内容及原因。
- 可能影响的已有功能。
- 添加的测试。
- 本地检查结果。
- 适用时提供实机验证结果及截图或录屏。
- 尚未验证的场景或已知限制。

自动检查失败，或涉及真实运行环境但缺少必要验证的 PR，通常需要补充后再合并。
