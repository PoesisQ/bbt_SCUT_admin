# SCUT 课堂助手 0.6

在原有学校字幕下载工具上增加本地语音服务、实时字幕、课堂提醒和知识点笔记。Edge 优先，使用 Chrome/Edge 共用的 Manifest V3 API。原版字幕下载和 CLI 均保留。

## 启动

**最方便的入口：**双击项目内的 `启动课堂助手.cmd`。服务启动后，打开 [三步安装向导](http://127.0.0.1:8765/welcome.html)。扩展目前是本地开发版，**不能在 Edge 商店搜索安装**。

这台电脑的扩展目录是 `S:\bbt_SCUT_admin-main\extension`。其他电脑选择自己项目内的 `extension` 目录。在 Edge 地址栏输入 `edge://extensions`，打开开发者模式 → 加载解压缩的扩展 → 选择这个目录（不是整个项目，也不是 ZIP）。扩展卡片名为 **SCUT 课堂助手**，图标为浅色纸张上的多彩水彩书页与声波。加载后固定到工具栏。

已有旧版时，先在扩展中“结束并保存”，等暂存上传为 0，再在扩展管理页点该扩展的“重新加载”，并刷新课程播放页。刷新扩展会销毁录音上下文，不要在上课录音中操作。本地面板直接刷新即可显示新的界面；打开或关闭本地面板不影响录音。

在项目目录运行：

```powershell
.\start-assistant.ps1
```

启动器使用 `uv` 创建本项目 `.venv`，安装锁定依赖，隐藏启动本地服务并打开面板。服务只监听 `127.0.0.1:8765`，没有开机启动任务。已经在运行时重复启动不会创建第二份服务。开发调试可运行 `uv run --extra asr python -m assistant_service`。

1. 打开 `edge://extensions`，启用开发者模式，选择“加载解压缩的扩展”，目录选本项目的 `extension`。Chrome 使用 `chrome://extensions`。
2. 在 [本地设置页](http://127.0.0.1:8765/options.html) 点击“复制扩展连接口令”。也可从 `.local/connection.txt` 复制。
3. 打开扩展设置页，粘贴口令，点击“连接”。浏览器扩展与本地面板的登录凭证分别保存，首次需要配对。
4. 模型、引擎、FFmpeg 路径自动探测 PotPlayer 的当前用户目录。确认后可点击“预加载模型并检查”。
5. 按需填写 DeepSeek Key 和模型名，保存。Key 加密存放在 Windows 用户目录的 SCUTClassroomAssistant/credentials.json，与代码目录分开；重启后继续使用，留空不会清除已有 Key。环境变量 `DEEPSEEK_API_KEY` 优先。

设置页分为“连接与安装、语音识别、智能分析、提醒与存储”四类，模型文件路径位于语音识别的高级选项中。底部保存栏会提示未保存的修改；桌面通知权限在扩展内单独即时设置。

界面使用浅色磨砂导航、柔光、圆角卡片、分段切换、声波、卡片进入和开关回弹动画，并尊重系统的“减少动态效果”偏好。[扩展外观预览](http://127.0.0.1:8765/study-popup.html?preview=1) 只展示界面，不会录音或创建课堂任务。

停止服务：先结束录音并等待上传完成，再运行 `.\stop-assistant.ps1`。该脚本仅停止由本项目启动并核实了路径与命令行的服务进程。

## 上课时

正常登录百步梯学堂，打开具体课时并播放声音，点击扩展的“开始实时字幕”。仅捕获所选课程标签页的声音，不捕获麦克风或其他应用声音。浏览器会显示其录音状态标记。捕获后通过 Web Audio 继续播放声音，避免 `tabCapture` 导致原标签页静音。

### 连续阅读与最小化

点击扩展中的 **“返回正在录音的课堂”**，打开独立课堂窗。字幕按时间顺序持续追加，可往上翻前文；向上阅读会暂停跟随，点击“回到最新”再追踪当前句子。另一个标签页保存点名、作业等课堂重点。这个窗口可最小化；再次点击返回按钮会复用并恢复它。关闭窗口也不结束录音，只有“结束录音并保存”会结束。

点击 Edge 的扩展菜单中 **SCUT 课堂助手这个名称**，或点击固定到工具栏的助手图标，只会打开操作面板，不会重启录音。0.5 连查询状态时新建空录音后台的行为也移除了。实况窗里的 **“本课详情与总结”** 直接打开这一节记录；**“课程笔记”** 用于查看以前的课程。点击课时即可进入笔记详情，查看完整字幕、提醒和总结，不用重新开始录音。关掉详情页面也不会删除记录。

工具栏的小弹窗是浏览器的临时页面，失去焦点时会关闭，但真正录音在 offscreen 后台，服务中的课堂也一直存在。0.4 修复了小弹窗每次打开把 DeepSeek 开关画成关闭的问题：现在以正在进行的课堂状态为准，并保存下次选择。默认偏好为开启，手动关闭会被记住；未配置 Key 时只使用本地字幕。开关在录音中修改立即作用于本课的后续分析，不重启录音。已发出的分析请求可能完成。

网页浮层有“精简字幕 / 展开上下文 / 最小化”三种状态。精简模式限制显示高度；上下文模式保留最近 24 条带时间戳字幕，完整记录在课堂窗。独立窗中的“字幕浮层”可重新附加当前录音的浮层。

[本地课堂实况](http://127.0.0.1:8765/classroom.html) 也能连续阅读与切换分析模型。它是普通本地网页，录音控制仍由扩展负责，右上角按钮返回课程笔记。

### 切换模型和修复重复字幕

- **语音模型：**偏好设置 → 语音识别 → 语音模型。列表只读取本机已经存在的 `model.bin`，当前机器有 large-v3-turbo 和 large-v3。选择后点“保存设置”，下一段识别开始使用；首次切换需要加载模型。路径也可在高级选项中填写。
- **语言：**默认“自动识别”，适合中英文交替课堂。2026-09-08 的实测中，固定中文把英语语音解码成大量重复汉字；同一音频自动检测为英语后恢复了正常句子。这里是按音频窗口检测语言，并不承诺一个窗口内所有语言都能准确识别。
- **DeepSeek：**课堂窗内可直接切换 V4 Flash / V4 Pro，下一次分析使用新模型；偏好设置 → 智能分析还支持自定义模型名。切换模型不自动重算历史笔记，整课重新分析可在课程笔记中发起。
- **异常防护：**只对极端字符循环、或伴随不可能短时间戳的连续句子循环重试。短口吃、少量重复、正常时长的重复讲解保留。重试仍异常或语言判断仍模糊时显示“识别不可靠”，保留该段音频和 `.asr.json` 诊断记录，不把它交给分析模型当作课堂事实。
- **修复已保存内容：**课程笔记 → 选中课时 → “更多 → 重新转写留存音频”。使用尚未清理的本课音频，先备份原始会话，再替换对应时间片段；实时录音优先。原始音频已清理时，需从学校回放重新加入处理。修复会清空旧概览并移除被替换片段对应的旧提醒；完成后可重新分析整课。

- 使用 16 kHz 单声道 PCM、8 秒窗口、1 秒重叠；常驻 Whisper 模型串行识别，避免多个模型挤占显存。
- 悬浮字幕可拖动、调字号、最小化；播放器容器全屏时跟随容器。浏览器原生 `<video>` 元素全屏可能无法容纳网页浮层，使用网站的播放器全屏即可。
- 本地规则快速标记点名/签到、扫码、提问、作业、测验、课程要求、安排、评分与特别提醒。规则结果明确标注“疑似”。
- 勾选 DeepSeek 后，默认每约 35 秒发送新的字幕及邻近上下文，结合语义分析；与识别使用独立队列。结束课时后还会分析尾段并生成整课分段概览。
- 桌面通知可在扩展设置启用；需要浏览器通知权限，且受 Windows 勿扰模式影响。通知只提示，不自动操作学校网页。
- 音频先写入扩展 IndexedDB，再按序上传；本地服务断开时自动重试。暂存超过约 14 分钟音频会停止继续捕获并提示恢复上传，避免无限占用存储。
- 服务按 `session + seq` 去重。结束录制前检查所有音频序号，缺段时拒绝把任务标为完整。扩展“恢复暂存音频上传”可补传浏览器重启后残留的录音。
- 请不要在后台浏览器设置中启用对课堂标签页的强制休眠；机器睡眠或关闭浏览器会中断实时捕获。已上传内容保留。

实时识别是短窗口增量转写，不是逐字无延迟同传。延迟包括窗口等待、识别和（可选）网络分析。语音中的提醒不能覆盖只在 PPT 出现、教师没有口头提及的二维码或要求；此版未实现截图 OCR 或视频视觉分析。

## 回放与批处理

点击“课程笔记 → 导入课程回放”，选择已登录的课程标签页，读取目录、勾选课时，然后开始处理。

- 默认优先导入学校字幕；没有字幕才使用回放视频源和本地 Whisper。勾选“重新转写”可强制使用本地模型。
- 自动识别学校 `sub_status`：直播、未开始、回放生成中和无回放的课时不进入回放队列。已完成回放常见状态为 `6`。
- 校内和 WebVPN 分别在自己的已登录标签页内读取接口，登录 Cookie 不导出到本地服务。不要在目录导入完成前关闭或切换到另一门课；导入完毕后可关闭面板，本地任务继续运行。
- 后台任务串行下载、提取音轨和分片转写，实时音频优先于回放分片。每课保留独立状态、进度和错误。停止导入与取消已入队课时是两个操作。
- 支持学校 MP4 等普通音视频源和普通未加密 VOD HLS。每层清单、分片、初始化段和重定向均检查域名。加密 HLS、特殊独立音轨/字节范围清单遇到时明确提示使用正常播放下的标签页捕获。
- 视频签名或学校登录过期导致源失效时，需要回到课程页面重新加入任务获取新地址。单纯重试已过期的地址不会刷新签名。
- 当前媒体下载上限为每课 4 GB、解码最长六小时。超限会报告错误，不把截断的内容作为完整课时保存。
- 每课可选 DeepSeek 总结，按时间顺序呈现主题、明确提到的教材章节和重点事项。没有明确章节时显示“原文未明确教材章节”。长课程按有界文本块分段分析，保留全部段落，不只分析开头。
- 从课时详情导出 ZIP，包含 SRT、TXT、学校兼容 JSON、事件 JSON、Markdown 笔记和完整会话 JSON。

回放的字幕时间戳以原视频为基准，可点击跳转，或在同一课时页面附加悬浮字幕。直播捕获时间从开始捕获时计时，不能直接与之后完整回放对齐；界面明确区分。回放边播放边识别需要 1 倍速；暂停、跳转或变速会结束本次录制以防时间轴错位，继续播放后可重新开启。后台批量转写没有这个限制。

## 数据和密钥

所有新文件均位于项目 `.local` 内，不修改 PotPlayer 引擎或模型：

```text
.local/
  settings.json         模型配置、随机本地连接口令（Key 独立保存在 Windows 用户目录）
  connection.txt        扩展配对说明
  assistant.sqlite3     WAL 模式的课程与持久化任务队列
  sessions/<id>/        每课字幕、事件、笔记；处理中临时音频
  server.log            服务输出
  server-error.log      本地错误输出
```

`.local/`、`.env` 和个人连接文件已加入 gitignore。成功处理后默认清理仅由本程序创建的临时音频；失败任务的输入保留用于重试，可在设置中选择保留全部音频。

本地 API 校验 Host、Origin 与随机 Bearer 口令。浏览器内的本地面板通过 HttpOnly/SameSite=Strict Cookie 和 same-origin 检查连接；学校页面不能调用本地 API。扩展口令保存在限制为 TRUSTED_CONTEXTS 的 storage 中，不注入网页。API Key 仅由本地服务使用，在 Windows 中用当前用户 DPAPI 加密；课程页面和扩展不持有 DeepSeek Key。发往 DeepSeek 的请求仅包含用户勾选分析的课时文字、片段 ID、时间戳和固定分析指令；不发送音频、学校 Cookie 或学校源地址。

## 验证与诊断

```powershell
uv run python scripts/check.py
# 真实 GPU 冒烟：会加载已配置模型，不会下载模型
uv run --extra asr python scripts/verify_asr.py PATH_TO_LOCAL_WAV
```

自动检查包括原版 CLI/字幕转换回归、本地鉴权、消息数据结构、序号去重与缺段、任务失败/重试/重启、实时优先级、规则去重、模型输出证据核验、源地址与 HLS 嵌套校验、PCM 重叠与尾段以及全部 JavaScript 语法。

此机器的常驻引擎使用 CTranslate2 4.4 配合已有 CUDA 12/cuDNN 8 DLL；这是 Faster Whisper 官方说明的兼容组合。锁定 setuptools<81 是为了旧版 CTranslate2 使用的 `pkg_resources`。首次加载模型约 6 秒，合成中文 25 秒音频识别约 2 秒；真实课堂 30 秒片段已验证本地 API、GPU 转写和原视频时间戳导出。测试片段在时间线中明确标注，不能代表整节课已处理。

Windows 实机测试另复现了 CTranslate2 4.4 在随机采样后释放模型时的 `0xC0000409` 问题。常驻引擎固定 `temperature=0.0` 使用确定性 beam search，避免默认温度回退触发有问题的 CUDA 随机状态；真实课堂片段再次识别约 1.3 秒，显式卸载模型及进程退出均正常。参见 [上游问题](https://github.com/SYSTRAN/faster-whisper/issues/1293) 和 [curand 释放修复](https://github.com/OpenNMT/CTranslate2/pull/1912)。

遇到无字幕：先检查播放器是否真正有声音。静音片段返回零条字幕属于正常行为。语音术语识别偏差可通过热词辅助，仍须核对原文。DeepSeek 网络/余额/Key/输出错误不影响本地字幕保存，可在课时详情重新分析。

## 依据

- [Chrome 官方：tabCapture + offscreen 音频录制](https://developer.chrome.com/docs/extensions/how-to/web-platform/screen-capture)
- [Microsoft Edge 扩展 API 支持](https://learn.microsoft.com/en-us/microsoft-edge/extensions/developer-guide/api-support)
- [Faster Whisper GPU 依赖与 CTranslate2 版本兼容](https://github.com/SYSTRAN/faster-whisper#gpu)
- [DeepSeek JSON Output](https://api-docs.deepseek.com/guides/json_mode/)
- [DeepSeek 当前模型文档](https://api-docs.deepseek.com/quick_start/pricing/)

网站研究记录见 `SITE_FINDINGS.md`；严格区分实际观察和仍待浏览器联调的环节。
