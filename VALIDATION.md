# 0.5 验证记录

本轮更新：2026-09-08，**45 项 Python + 19 项 JavaScript，共 64 项回归通过**。记录在 `.local/smoke/navigation-checks.log`。

- 新增回归证明：多次查询状态不会创建 offscreen；录音中打开面板或关闭其他课堂窗口，不发送第二次 START，也不发送 STOP；关闭实际课程源标签页仍会停止并保存。
- 新增本课详情链接回归，只打开指定 session 的详情，不调用捕获消息。
- Edge 本地页面实际验证：实况页明确说明关闭行为；“本课详情与总结”链接选中同一节已保存课时，能打开全部 102 条字幕；页面无水平溢出。
- 新版快捷面板预览高度约 582 px，返回课堂、所有课程和使用说明入口可见。仍属于本地预览，不能当成受保护扩展页的实点验证。
- 图标改为内置 image_gen 生成的水彩书页 / 声波，保留颜料纹理与细墨线。原始图、生成说明及 PNG 尺寸资源位于 `assets/identity` 和 `extension/icons`。
- 本地服务已更新至 0.5.0。维护前确认课程录音均已结束、队列空闲；没有为了验证 UI 重新采集用户音频。
- 待发布文件已检查不包含已配置的连接口令或 DeepSeek Key；`.local` 内的音频、课堂转写、诊断和数据库均不在 Git 中。

扩展页自动化限制、Chrome 录音与批处理实机覆盖范围仍见下面的边界说明。README 新增原项目致谢与“打开 / 关闭 / 重新加载”操作表。

# 0.4 历史验证记录

环境：2026-09-08，Windows，RTX 4060 Laptop 8 GB，Python 3.12，PotPlayer 已安装的 large-v3-turbo、CUDA 12/cuDNN 8。引擎和模型目录仅被读取。

## 重复字幕的实际原因与修复

- 用户截图中的长串汉字可在当前神经科学课的已保存音频中复现。原来配置固定 `language=zh`，教师使用英语时，解码出现极端汉字重复。
- 对同一批原始音频改用自动语言识别后，多段检测为英语，置信度约 0.96–0.998，输出恢复为正常英语句子。因此至少这些样本的主要问题是语言约束与语音不匹配；不能据此认定所有字幕错误都只有这个原因。
- 当前本地配置和新用户默认值均已改为自动语言识别。保留 `temperature=0.0`，避免这台 Windows/CUDA 环境已经复现过的随机采样卸载崩溃。
- 新增异常检测及有限重试。除单句极端循环外，还覆盖拆成多条字幕、出现多个 0.02 秒长句的异常时间戳情况。正常时长的多次重复讲解、短口吃和少量词语重复保留。
- 重试可能强迫模型生成流畅却不相关的文字，因此病态解码之后仍无法明确判断语言的片段被标记为待复核，不进入语义分析；保留音频和识别尝试的审计文件。
- 对正在录制的课堂，已备份原始会话，并实际重新转写 352 段已保存音频。之后单独重试了一个跨字幕重复的片段，最终因语言不确定标记为待复核。修复队列未出现失败任务。
- 修复旧片段按原始音频片段 ID 替换，保留时间顺序；不会将旧片段错误地与未来句子去重。旧提醒的关联片段和过时分析响应都有失效处理。
- 原始诊断样本、对比脚本、对比结果与会话备份均在被 gitignore 排除的 `.local` 下，发行包不包含课堂内容。

## 录音、分析与界面

- 用户已在 Edge 安装扩展。本次通过实际课程的音频接收和服务任务确认 `tabCapture → offscreen → 本地服务 → GPU` 链路正在工作；维护期间采集会话未被停止，上传序号继续增长。
- DeepSeek Key 已由用户配置，当前课堂 `analysis=true`，能观察到真实 DeepSeek 任务完成和带原文依据的提醒。早期 0.3 记录中“尚未配置 Key / 尚未安装扩展”的状态已过时。
- 原小弹窗未持久化开关导致重新打开显示关闭，实际录音及本课分析并未因此重启。现在从本课真实状态恢复开关，并记住下一节课的偏好；新偏好默认为开启，用户手动关闭仍保留。
- Edge 本地课堂实况已实际显示数百条连续字幕、最新句子、时间戳与课堂重点。新文字持续追加，DeepSeek 状态为开启，模型为 V4 Flash，页面无水平溢出。
- Edge 本地设置页实际列出本机 large-v3 / large-v3-turbo、自动语言选项和 DeepSeek V4 Flash / V4 Pro / 自定义模型名；没有为了测试而切换用户正在使用的语音模型或额外发起付费模型请求。
- 独立课堂窗的创建、复用及从最小化恢复有自动回归，不调用开始录音；浮层新增精简、上下文和最小化三态。新版工具栏弹窗、设置、课堂窗共享折页图标。
- 新图标为可编辑 SVG 几何图形，并提供 16/32/48/128/512 PNG。`scripts/generate_icon.ps1` 可重建 PNG，未引入外部素材服务。

## 自动回归

`uv run python scripts/check.py`：**45 项 Python、16 项 JavaScript，共 61 项通过**，另通过 JavaScript 语法检查、Python 编译及 git diff 格式检查。日志在 `.local/smoke/final-checks.log`。

覆盖原版字幕解析与 CLI、鉴权和密钥隔离、音频序号去重与缺段、任务中断恢复、优先级、分析证据校验、媒体/HLS 来源限制、AudioWorklet 重叠与尾段；新增语言重试、病态循环、跨句循环、低置信度回退、正常重复保留、旧片段替换、定向修复、开关默认值与持久化、课堂窗复用等回归。

此前已实测学校操作系统回放 30:00–30:30 的音频通过 GPU/API 生成 27 条带原视频时间戳的字幕，SRT/TXT/JSON 正常导出；Edge 和 Chrome 的本地面板均能显示。该记录在课程空间明确标为 30 秒验证片段，不能代表整课批处理已通过实机验证。

## 尚未实机验证的部分

- 当前浏览器自动化的安全策略拒绝控制 `chrome-extension://` 页面。本次没有绕过限制，因此新版扩展独立窗按钮、真实最小化、浮层全屏和 Edge 扩展重新加载没有通过工具实点验证。已进行代码/自动回归和同源本地 UI 验证，不能把本地预览当作扩展实测。
- Chrome 扩展录音仍待实机验证。学校目录批处理的完整浏览器流程、不同课程的全部媒体格式也未穷尽验证。
- 完整 large-v3 和 DeepSeek V4 Pro 本轮没有实际加载/调用；选择入口与配置保存共用已验证配置 API，切换后的质量和耗时需按课堂音频评估。
- 升级正在安装的扩展前，用户需先结束并保存录音、等上传为 0，再重新加载扩展并刷新课程页。浏览器的临时弹窗和新独立课堂窗是两个不同 UI 生命周期，录音由 offscreen 持有。

## 功能边界

识别仍可能误听术语、口音或静音噪声；异常防护不能保证字幕逐字正确。自动语言检测以音频窗口为单位。只处理音频和文字，没有截图 OCR、PPT 二维码视觉识别或自动课堂签到。直播捕获时间与原始回放时间分开标记。加密/特殊 HLS 使用正常播放器捕获音频；不绕过学校认证和权限。
